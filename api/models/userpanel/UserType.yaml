components:
  schemas:
    UserType:
      type: object
      properties:
        id:
          type: integer
          format: int32
          minimum: 0
        title:
          type: string
          x-faker: lorem.word
          example: "Admin"
      required:
        - id
        - title
