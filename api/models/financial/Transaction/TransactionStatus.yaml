components:
  schemas:
    TransactionStatus:
      type: integer
      format: int32
      minimum: 0
      enum:
        - 1 #UNPAID
        - 2 #PAID
        - 3 #REFUND
        - 4 #EXPIRED
        - 5 #REJECTED
        - 6 #PENDING
      description: >
      